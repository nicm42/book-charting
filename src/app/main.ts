import './style.css';
import data from '../../data.json';

const getData = async () => {
  /* const response = await fetch('/api');
  const data = await response.json(); */
  console.log(data);
};

getData();
